export async function GET(){return Response.json({ok:true,api:'send-telegram',telegramBotTokenConfigured:Boolean(process.env.TELEGRAM_BOT_TOKEN),telegramChatIdConfigured:Boolean(process.env.TELEGRAM_CHAT_ID)})}
export async function POST(request){const botToken=process.env.TELEGRAM_BOT_TOKEN,chatId=process.env.TELEGRAM_CHAT_ID;if(!botToken||!chatId)return Response.json({ok:false,error:'Telegram env not configured'},{status:500});const incoming=await request.formData();const documentFile=incoming.get('document');const caption=incoming.get('caption')||'Расчёт мягких окон';const form=new FormData();form.append('chat_id',chatId);form.append('caption',caption);form.append('document',documentFile);const r=await fetch(`https://api.telegram.org/bot${botToken}/sendDocument`,{method:'POST',body:form});return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json; charset=utf-8'}})}
